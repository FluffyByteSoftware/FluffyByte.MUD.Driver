DateTime Notes:
Always prefer to use times in Utc so there's no need to worry about DST.

NetworkDaemon - The network daemon controls the network and handles all incoming connections.
Switchboard - The switchboard handles tracking all connections, and routing information to those connections, as well as
    ticking them at 750 ms for input and 1.5 seconds for outputs.
 
 
 psql
 
 driver / pd6zAaym3FH6Ej
 